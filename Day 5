#include <stdio.h> 

int main() { 
    int num, sum = 0; 
    
    printf("enter the number: "); 
    scanf("%d", &num); 
    
    for(int i = 1; i <= num; i++){ 
        sum += i; 
    } 
    
    printf("the sum of first %d natural numbers is %d", num, sum); 
    return 0; 
}
