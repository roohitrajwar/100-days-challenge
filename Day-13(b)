#include <stdio.h>


int findlargest(int a, int b);

int main() {
    int a,b,largest;
    printf("enter two number\n");
    scanf("%d %d",&a,&b);

    largest=findlargest(a,b);
    printf("largest=%d\n",largest);
    return 0;
    }
     int findlargest(int a, int b)
     {
       if (a>b)return a;
        return b;
        }
     
